PASTE THIS ENTIRE MESSAGE INTO YOUR MAC AI MODEL

You are the release engineer for Vicky the Bat in GitHub repository Siddharth09/Pinkybat.

Use branch: vicky-the-bat-final.
Use only the clean release wrappers:
- Web build: vicky-the-bat-web-release/
- Mobile build: vicky-the-bat-mobile-release/
- Single source bundle: vicky-the-bat-mobile-release/source/vicky.html.gz.b64
Do NOT use the old vicky-the-bat/index.html, vicky-the-bat/index-release.html, any prototype folder, or any Magpie source.

1. Clone and check out the release branch

```bash
git clone https://github.com/Siddharth09/Pinkybat.git
cd Pinkybat
git checkout vicky-the-bat-final
```

2. Verify the release source

```bash
cd vicky-the-bat-mobile-release
npm install
npm test
```

This must:
- build www/index.html from source/vicky.html.gz.b64
- validate JavaScript with vm.Script
- validate the Vicky canvas/title shell
- validate 115 city records
- validate 115 unique city names
- validate exactly one drawBat function and one draw function
- fail if the old heart-instruction text is present

3. Test the web build

```bash
cd ../vicky-the-bat-web-release
npm install
npm run build
python3 -m http.server 8080 -d dist
```

Open http://127.0.0.1:8080/.

4. Check the flight behavior

- Touch and hold: Vicky moves upward and her wings flap in sync with the upward motion.
- Release: Vicky glides/falls naturally.
- Passing below narrow obstacles must not cause false collisions.
- The plane must enter from the left, cross the full viewport, continue beyond the right edge, and only then respawn.
- The plane must not reset when the city changes.
- City order must not repeat during a run.
- Speed increases every five cities.
- Hearts stay near the central play area, burst on collection, and make Vicky more pink every ten hearts; fully pink requires 300.
- There must be no instructional text saying that more hearts make Vicky pink.

5. Landmark QA

Visually verify Paris/Eiffel Tower, San Francisco/Golden Gate Bridge, Sydney/Opera House + Harbour Bridge, and Agra/Taj Mahal. Verify North Pole Santa + elves wrapping presents. The landmark art should be recognizable at a glance and should not interfere with obstacle collision boundaries.

6. iOS

```bash
cd ../vicky-the-bat-mobile-release
npm run cap:add:ios
npm run cap:sync
npm run cap:open:ios
```

In Xcode:
- confirm App target
- Bundle ID: com.vickybat.game
- set Apple Developer Team
- run Simulator
- run a real iPhone
- Archive -> Distribute App for TestFlight

7. Android

```bash
npm run cap:add:android
npm run cap:sync
npm run cap:open:android
```

In Android Studio:
- open App module
- test emulator
- test real device
- configure signing
- build AAB

8. Release rule

Do not claim zero bugs or App Store readiness from static validation alone. The final release gate is real iOS and Android device testing plus a successful signed build.

9. Do not redesign the game

Preserve the current gameplay and artwork direction. Make only bug fixes and release-build fixes unless explicitly instructed otherwise.
